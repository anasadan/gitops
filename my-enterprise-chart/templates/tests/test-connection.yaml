apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-enterprise-chart.fullname" . }}-test-connection"
  labels:
    {{- include "my-enterprise-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-enterprise-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
