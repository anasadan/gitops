apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-enterprise-chart.fullname" . }}
  labels:
    {{- include "my-enterprise-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-enterprise-chart.selectorLabels" . | nindent 4 }}
